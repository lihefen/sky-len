import { navigateTo } from '@/utils';
import { get } from 'lodash';

export default {
    components: {
    },
    name: 'HomeApp',
    data() {
        return {
        };
    },
    mounted() {
    },
    methods: {
    }
};